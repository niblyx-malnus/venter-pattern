/-  *venter
|%
++  enjs
  =,  enjs:format
  |%
  ++  vent
    |=  =^vent
    ?-  -.vent
      %ack     s+%ack
      %new-id  (frond %new-id s+(scot %uv id.vent))
    ==
  --
++  dejs
  =,  dejs:format
  |%
  ++  action
    ^-  $-(json ^action)
    %-  of
    :~  [%create-datum (ot ~[t+so])]
        [%delete-datum (ot ~[id+(cu (cury slav %uv) so)])]
    ==
  --
--
